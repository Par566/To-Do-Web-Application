const Navbar = () => {
  return (
    <nav className="navbar">
      <h2 className="logo">Todo App</h2>
      <div className="nav-links">
        <span>Login</span>
        <span>Register</span>
      </div>
    </nav>
  );
};

export default Navbar;
